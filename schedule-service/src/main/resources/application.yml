spring:
  jpa:
    show-sql: true
  application:
    name: schedule-service
  cloud:
    config:
      uri: http://localhost:8085
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8888/eureka/
management:
  security:
    enabled: false
